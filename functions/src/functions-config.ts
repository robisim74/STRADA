/**
 * Functions configuration.
 */
export const functionsConfig = {
    apis: {
        googleMaps: {
            serverKey: 'YOUR_API_KEY'
        }
    },
    whitelist: [
        'http://localhost:4200'
    ]
};
